<UserControl
    x:Class="Beats.AudioControls.RecordFromInput"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Beats.AudioControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ListBox ItemsSource="{Binding RecordingDevices}"
                 SelectedItem="{Binding SelectedInputDevice, Mode=TwoWay}"
                 DisplayMemberPath="Name"
                 Margin="10"/>
        <StackPanel>
            <Button Content="{Binding TransportAction}" Click="ButtonBase_OnClick"></Button>
        </StackPanel>
    </Grid>
</UserControl>
